import { styled } from '@mui/system';

const FilterStyles = styled('aside')({
  display: 'flex',
  flexDirection: 'column',
  borderRight: '1px solid #EBEBEB',
  margin: '0 auto',
  paddingLeft: '10px',
});

export default FilterStyles;
